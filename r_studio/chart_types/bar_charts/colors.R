# Vector of n contiguous colors

# Use rainbow colors
barplot(1:5, col = rainbow(5))

# Use heat colors
barplot(1:5, col = heat.colors(5))

# Use terrain colors
barplot(1:5, col = terrain.colors(5))

# Use topographic colors
barplot(1:5, col = topo.colors(5))

# Use tundra colors
barplot(1:5, col = cm.colors(5))
